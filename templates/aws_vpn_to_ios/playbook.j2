- hosts: peer_a
  connection: local
  gather_facts: no

  tasks:
    - cloudformation:
        stack_name: {{ cloud_network_interconnect_name }}-stack
        template: peer_a.config


- hosts: peer_b
  gather_facts: yes

  tasks:
    - ios_config:
        src: peer_b.config
